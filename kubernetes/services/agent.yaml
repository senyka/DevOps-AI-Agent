apiVersion: v1
kind: Service
metadata:
  name: agent
  namespace: devops-agent
spec:
  selector:
    app: agent
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
