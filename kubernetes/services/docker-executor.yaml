apiVersion: v1
kind: Service
metadata:
  name: docker-executor
  namespace: devops-agent
spec:
  selector:
    app: docker-executor
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      name: http
  type: ClusterIP
