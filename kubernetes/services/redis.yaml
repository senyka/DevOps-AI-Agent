apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: devops-agent
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
      name: redis
  type: ClusterIP
